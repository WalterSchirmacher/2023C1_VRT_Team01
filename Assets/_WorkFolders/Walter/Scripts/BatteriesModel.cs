using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Normal.Realtime;
using Normal.Realtime.Serialization;

[RealtimeModel]
public partial class BatteriesModel
{
    [RealtimeProperty(1, true, true)]
    private bool _vis;

    [RealtimeProperty(2, true, true)]
    private bool _blowUp;
}

/* ----- Begin Normal Autogenerated Code ----- */
public partial class BatteriesModel : RealtimeModel {
    public bool vis {
        get {
            return _visProperty.value;
        }
        set {
            if (_visProperty.value == value) return;
            _visProperty.value = value;
            InvalidateReliableLength();
            FireVisDidChange(value);
        }
    }
    
    public bool blowUp {
        get {
            return _blowUpProperty.value;
        }
        set {
            if (_blowUpProperty.value == value) return;
            _blowUpProperty.value = value;
            InvalidateReliableLength();
            FireBlowUpDidChange(value);
        }
    }
    
    public delegate void PropertyChangedHandler<in T>(BatteriesModel model, T value);
    public event PropertyChangedHandler<bool> visDidChange;
    public event PropertyChangedHandler<bool> blowUpDidChange;
    
    public enum PropertyID : uint {
        Vis = 1,
        BlowUp = 2,
    }
    
    #region Properties
    
    private ReliableProperty<bool> _visProperty;
    
    private ReliableProperty<bool> _blowUpProperty;
    
    #endregion
    
    public BatteriesModel() : base(null) {
        _visProperty = new ReliableProperty<bool>(1, _vis);
        _blowUpProperty = new ReliableProperty<bool>(2, _blowUp);
    }
    
    protected override void OnParentReplaced(RealtimeModel previousParent, RealtimeModel currentParent) {
        _visProperty.UnsubscribeCallback();
        _blowUpProperty.UnsubscribeCallback();
    }
    
    private void FireVisDidChange(bool value) {
        try {
            visDidChange?.Invoke(this, value);
        } catch (System.Exception exception) {
            UnityEngine.Debug.LogException(exception);
        }
    }
    
    private void FireBlowUpDidChange(bool value) {
        try {
            blowUpDidChange?.Invoke(this, value);
        } catch (System.Exception exception) {
            UnityEngine.Debug.LogException(exception);
        }
    }
    
    protected override int WriteLength(StreamContext context) {
        var length = 0;
        length += _visProperty.WriteLength(context);
        length += _blowUpProperty.WriteLength(context);
        return length;
    }
    
    protected override void Write(WriteStream stream, StreamContext context) {
        var writes = false;
        writes |= _visProperty.Write(stream, context);
        writes |= _blowUpProperty.Write(stream, context);
        if (writes) InvalidateContextLength(context);
    }
    
    protected override void Read(ReadStream stream, StreamContext context) {
        var anyPropertiesChanged = false;
        while (stream.ReadNextPropertyID(out uint propertyID)) {
            var changed = false;
            switch (propertyID) {
                case (uint) PropertyID.Vis: {
                    changed = _visProperty.Read(stream, context);
                    if (changed) FireVisDidChange(vis);
                    break;
                }
                case (uint) PropertyID.BlowUp: {
                    changed = _blowUpProperty.Read(stream, context);
                    if (changed) FireBlowUpDidChange(blowUp);
                    break;
                }
                default: {
                    stream.SkipProperty();
                    break;
                }
            }
            anyPropertiesChanged |= changed;
        }
        if (anyPropertiesChanged) {
            UpdateBackingFields();
        }
    }
    
    private void UpdateBackingFields() {
        _vis = vis;
        _blowUp = blowUp;
    }
    
}
/* ----- End Normal Autogenerated Code ----- */
